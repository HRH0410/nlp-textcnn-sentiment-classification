data:
  data_dir: preprocessed_file
  vocab_file: tokens2id.csv
  max_len: 60
  num_classes: 5

model:
  name: textcnn
  embedding_dim: 300
  num_filters: 100
  filter_sizes: [3, 4, 5]
  dropout: 0.5

training:
  seed: 42
  epochs: 20
  batch_size: 64
  learning_rate: 0.001
  weight_decay: 0.0001
  num_workers: 2

output:
  output_dir: outputs
  checkpoint_dir: checkpoints
